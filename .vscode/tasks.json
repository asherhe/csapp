{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Test run Current Lab",
      "type": "shell",
      "command": "${workspaceFolder}/testlab.sh ${fileDirname} ${fileBasename}",
      "problemMatcher": [],
      "group": "build"
    }
  ]
}